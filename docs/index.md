---
title: daft - Git Extensions Toolkit
description:
  daft gives every branch its own directory - a plain git worktree with its own
  dependencies, build, and dev server - and keeps the whole set coherent across
  repos, for you and for the agents you run.
layout: home
hero:
  text: Stop switching branches. Run all of them.
  tagline:
    daft gives every branch its own directory — a plain git worktree with its
    own dependencies, build, and dev server — and keeps the whole set coherent
    across repos, for you and for the agents you run.
---
