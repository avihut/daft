#!/usr/bin/env bash
#MISE description="Benchmark: bash list tests vs YAML manual tests"
#MISE depends=["dev"]
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/_bench_command" "list" "test_list.sh" "list"
