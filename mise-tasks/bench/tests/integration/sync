#!/usr/bin/env bash
#MISE description="Benchmark: bash sync tests vs YAML manual tests"
#MISE depends=["dev"]
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/_bench_command" "sync" "test_sync.sh" "sync"
