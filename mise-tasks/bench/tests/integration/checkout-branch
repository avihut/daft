#!/usr/bin/env bash
#MISE description="Benchmark: bash checkout-branch tests vs YAML manual tests"
#MISE depends=["dev"]
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/_bench_command" "checkout-branch" "test_checkout_branch.sh" "checkout-branch"
