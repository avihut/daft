#!/usr/bin/env bash
#MISE description="Benchmark: bash checkout tests vs YAML manual tests"
#MISE depends=["dev"]
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/_bench_command" "checkout" "test_checkout.sh" "checkout"
