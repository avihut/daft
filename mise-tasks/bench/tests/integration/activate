#!/usr/bin/env bash
#MISE description="Benchmark: bash activate tests vs YAML manual tests"
#MISE depends=["dev"]
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/_bench_command" "activate" "test_activate.sh" "activate"
