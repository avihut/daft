#!/usr/bin/env bash
#MISE description="Benchmark: bash init tests vs YAML manual tests"
#MISE depends=["dev"]
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/_bench_command" "init" "test_init.sh" "init"
