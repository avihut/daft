#!/usr/bin/env bash
#MISE description="Benchmark: bash rename tests vs YAML manual tests"
#MISE depends=["dev"]
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/_bench_command" "rename" "test_rename.sh" "rename"
