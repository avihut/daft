#!/usr/bin/env bash
#MISE description="Benchmark: bash clone tests vs YAML manual tests"
#MISE depends=["dev"]
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/_bench_command" "clone" "test_clone.sh" "clone"
