#!/usr/bin/env bash
#MISE description="Benchmark: bash setup tests vs YAML manual tests"
#MISE depends=["dev"]
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/_bench_command" "setup" "test_setup.sh" "setup"
