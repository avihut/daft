#!/usr/bin/env bash
#MISE description="Benchmark: bash completions tests vs YAML manual tests"
#MISE depends=["dev"]
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/_bench_command" "completions" "test_completions.sh" "completions"
