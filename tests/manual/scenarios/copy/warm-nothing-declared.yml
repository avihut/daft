name: Warm with no copy declaration explains itself
description: >
  The first thing most users will see: `daft warm` in a repo whose daft.yml has
  no `copy:` section. It must be a zero-exit no-op that names the worktree whose
  config was read and says where to declare the caches — not an error, and not a
  silent success that leaves the user wondering whether anything happened.

repos:
  - name: test-repo
    use_fixture: standard-remote

steps:
  - name: Clone the repository
    run: daft clone --layout contained $REMOTE_TEST_REPO
    expect:
      exit_code: 0
      dirs_exist:
        - "$WORK_DIR/test-repo/main"

  - name: Create a second worktree
    run: daft go develop
    cwd: "$WORK_DIR/test-repo/main"
    expect:
      exit_code: 0
      dirs_exist:
        - "$WORK_DIR/test-repo/develop"

  # No daft.yml at all — the "nothing to do" case must not depend on a config
  # file existing.
  - name: Warming with nothing declared succeeds and says why
    run: daft warm develop 2>&1
    cwd: "$WORK_DIR/test-repo/main"
    expect:
      exit_code: 0
      output_contains:
        # Both ends named even here: the user asked to warm something, and
        # "nothing happened" is only useful if it says nothing happened *where*.
        - "No `copy:` paths declared in 'main'"
        - "'develop'"
        - "daft.yml"
      output_not_contains:
        - "Copied"

  # A VALID config that simply has no `copy:` key. The distinction matters:
  # a config that would not parse is a different message entirely
  # (warm-malformed-config.yml), and this scenario's earlier fixture was
  # accidentally the malformed case — hidden precisely because both used to
  # print "No `copy:` paths declared".
  - name: A daft.yml without a copy section is the same case
    run: |
      printf 'shared:\n  - .env\n' > daft.yml
      daft warm develop 2>&1
    cwd: "$WORK_DIR/test-repo/main"
    expect:
      exit_code: 0
      output_contains:
        - "No `copy:` paths declared in 'main'"
      output_not_contains:
        - "could not read the configuration"
