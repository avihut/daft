name: Layout hint not shown when --layout flag is used
description:
  When the user explicitly specifies a layout via --layout, the first-time
  layout hint should not be shown (the user already knows about layouts).

repos:
  - name: test-repo
    use_fixture: standard-remote

steps:
  - name: Clone with explicit --layout flag
    run: git-worktree-clone --layout sibling $REMOTE_TEST_REPO 2>&1
    expect:
      exit_code: 0
      output_not_contains:
        - "daft layout list"
