name: Merge cross-worktree
description: "daft merge <source> --into <target> works from any worktree"

repos:
  - name: test-repo
    use_fixture: standard-remote

steps:
  - name: Clone
    run: git-worktree-clone --layout contained $REMOTE_TEST_REPO
    expect:
      exit_code: 0

  - name: Check out feature
    run: git-worktree-checkout feature/test-feature
    cwd: "$WORK_DIR/test-repo"
    expect:
      exit_code: 0

  - name: Merge feature into main *from the feature worktree*
    run: git-worktree-merge --into main feature/test-feature --no-edit
    cwd: "$WORK_DIR/test-repo/feature/test-feature"
    expect:
      exit_code: 0
      output_contains:
        - "Merged feature/test-feature into main (commit "

  - name: Verify main contains feature commits without being CWD
    run: git -C $WORK_DIR/test-repo/main log --oneline
    cwd: "$WORK_DIR/test-repo/feature/test-feature"
    expect:
      exit_code: 0
      output_contains:
        - "Add feature branch"

  - name: Verify source worktree branch unchanged
    run:
      git -C $WORK_DIR/test-repo/feature/test-feature symbolic-ref --short HEAD
    cwd: "$WORK_DIR/test-repo"
    expect:
      exit_code: 0
      output_contains:
        - "feature/test-feature"
