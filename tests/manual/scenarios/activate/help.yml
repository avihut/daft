name: Activate help
description: Help names the mechanism (shell-init) and both flags

steps:
  - name: Show help
    run: daft activate --help 2>&1
    expect:
      exit_code: 0
      output_contains:
        - "shell-init"
        - "--dry-run"
        - "--force"
