name: Activate dry-run
description: --dry-run previews changes without modifying anything

steps:
  - name: Run activate with --dry-run
    run: |
      export HOME=$(mktemp -d)
      daft activate --dry-run 2>&1
    expect:
      exit_code: 0
      output_contains:
        - "[dry-run]"
