name: Checkout carry help
description: Help output shows --carry, --no-carry, and -c flags

steps:
  - name: Check help shows carry flags
    run: git-worktree-checkout --help 2>&1
    expect:
      exit_code: 0
      output_contains:
        - "--carry"
        - "--no-carry"
        - "-c"
