name: Mixed foreground and background hook invocation
description:
  "Regression for the universal hook invocation logging feature. A hook that
  declares both foreground and background jobs must produce a single invocation
  record whose rows are written by two different processes: the foreground jobs
  are written by the main daft process, the background jobs by the coordinator
  child. This scenario verifies that the writes are disjoint (no races), all
  four jobs appear in 'daft hooks jobs' under the same invocation, and 'daft
  hooks jobs logs <name>' returns the captured output for both fg and bg jobs.
  The fg job output is visible live on stdout; the bg job output is captured to
  the log store only."

repos:
  - name: test-mixed-fg-bg
    default_branch: main
    branches:
      - name: main
        files:
          - path: README.md
            content: "# Mixed fg+bg hook test"
        commits:
          - message: "Initial commit"
      - name: feature/mixed
        from: main
    daft_yml: |
      hooks:
        worktree-post-create:
          jobs:
            - name: setup-env
              run: echo fg-setup
            - name: print-done
              run: echo fg-done
            - name: warm-cache
              run: sleep 1 && echo bg-cache
              background: true
            - name: warm-index
              run: sleep 1 && echo bg-index
              background: true

steps:
  - name: Clone the repository
    run: git-worktree-clone --layout contained $REMOTE_TEST_MIXED_FG_BG
    expect:
      exit_code: 0
      dirs_exist:
        - "$WORK_DIR/test-mixed-fg-bg/main"
      files_exist:
        - "$WORK_DIR/test-mixed-fg-bg/main/daft.yml"

  - name: Trust the repository
    run: daft hooks trust --force 2>&1
    cwd: "$WORK_DIR/test-mixed-fg-bg/main"
    expect:
      exit_code: 0

  - name:
      Checkout feature branch (triggers mixed fg/bg worktree-post-create hook)
    run: env -u DAFT_TESTING git-worktree-checkout feature/mixed 2>&1
    cwd: "$WORK_DIR/test-mixed-fg-bg/main"
    expect:
      exit_code: 0
      dirs_exist:
        - "$WORK_DIR/test-mixed-fg-bg/feature/mixed"
      output_contains:
        - "fg-setup"
        - "fg-done"
      output_not_contains:
        - "bg-cache"
        - "bg-index"

  - name: Wait for background jobs to reach terminal state, then list jobs
    # Earlier versions of this loop just grep'd for the bg job name in
    # `daft hooks jobs` output. That's racy: the coordinator writes the
    # initial `Running` meta entry *before* invoking `run_command`, so
    # the entry appears in the listing while `sleep 1` is still pending —
    # subsequent steps (which inspect logs and expect "COMPLETED") then
    # see the entry mid-flight. With fork() the gap between meta-write
    # and subprocess-finish was small enough to mostly hide the race;
    # spawn-self adds ~200ms of binary-load latency that pushes step 7
    # past the cliff. Wait for the specific bg jobs to reach a terminal
    # status instead — that's what the next steps actually need.
    run: |
      for i in $(seq 1 30); do
        done=$(daft hooks jobs --format json 2>/dev/null | jq -r '
          [.[] | select(.name == "warm-cache" or .name == "warm-index") | .status]
          | if length == 2 and all(. == "completed" or . == "failed" or . == "cancelled")
            then "yes" else "no" end
        ' 2>/dev/null)
        if [ "$done" = "yes" ]; then
          daft hooks jobs 2>&1
          exit 0
        fi
        sleep 0.5
      done
      echo "BG_JOBS_TIMEOUT"
      daft hooks jobs 2>&1
      exit 1
    cwd: "$WORK_DIR/test-mixed-fg-bg/feature/mixed"
    expect:
      exit_code: 0
      output_contains:
        - "worktree-post-create"
        - "setup-env"
        - "print-done"
        - "warm-cache"
        - "warm-index"
        - "completed"
      output_not_contains:
        - "(no jobs declared)"
        - "No background job history found."

  - name: View log for a foreground job — written by main daft process
    run: daft hooks jobs logs setup-env 2>&1
    cwd: "$WORK_DIR/test-mixed-fg-bg/feature/mixed"
    expect:
      exit_code: 0
      output_contains:
        - "setup-env"
        - "fg-setup"
        - "COMPLETED"

  - name: View log for the second foreground job
    run: daft hooks jobs logs print-done 2>&1
    cwd: "$WORK_DIR/test-mixed-fg-bg/feature/mixed"
    expect:
      exit_code: 0
      output_contains:
        - "print-done"
        - "fg-done"
        - "COMPLETED"

  - name: View log for a background job — written by coordinator child
    run: daft hooks jobs logs warm-cache 2>&1
    cwd: "$WORK_DIR/test-mixed-fg-bg/feature/mixed"
    expect:
      exit_code: 0
      output_contains:
        - "warm-cache"
        - "bg-cache"
        - "COMPLETED"

  - name: View log for the second background job
    run: daft hooks jobs logs warm-index 2>&1
    cwd: "$WORK_DIR/test-mixed-fg-bg/feature/mixed"
    expect:
      exit_code: 0
      output_contains:
        - "warm-index"
        - "bg-index"
        - "COMPLETED"
