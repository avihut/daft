name: Unknown command suggestion
description: "daft activ suggests activate"

steps:
  - name: Run typo of activate
    run: daft activ 2>&1
    expect:
      exit_code: 1
      output_contains:
        - "activate"
        - "The most similar command"
